name: Deploy GitActions Rodando AWS K8s

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout do código
      uses: actions/checkout@v4

    
    - name: Construir imagem Docker
      run: docker build -t minha-imagem ./frontend